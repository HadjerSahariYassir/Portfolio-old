import {createMuiTheme} from '@material-ui/core';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#2196f3',
            
            
        },
        secondary:{
            main: "#1e88e5",
        },
       // type: 'dark',
    },
});

export default theme;